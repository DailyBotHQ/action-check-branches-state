import { expect } from 'chai'
// import GHActionRunner from '../src/index'


describe('Test GitHub Branches Checker', () => {

  describe('Using default options', () => {
    it('dummy test', () => {
      // (1) Smile 😀
      expect(true).to.be.equal(true)
    })
  })
})